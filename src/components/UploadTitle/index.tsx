import styled from "styled-components";

const UploadTitle = styled.h3`
  display: block;
  margin: 8px 0;
  font-weight: 700;
  font-size: 16px;
  line-height: 19px;
  text-align: center;
  color: var(--dark-blue);
`;

export default UploadTitle;

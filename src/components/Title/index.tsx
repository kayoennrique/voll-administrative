import styled from "styled-components";

const Title = styled.h1`
  font-weight: 700;
  font-size: 24px;
  line-height: 28px;
  color: var(--gray);
  text-align: center;
  margin: 1rem 0;
`;

export default Title;
